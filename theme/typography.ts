export const typography = {
  screenTitle: {
    fontSize: 20,
    lineHeight: 34,
    fontWeight: "700" as const,
  },

  sectionTitle: {
    fontSize: 12,
    lineHeight: 18,
    fontWeight: "700" as const,
  },

  cardTitle: {
    fontSize: 16,
    lineHeight: 22,
    fontWeight: "700" as const,
  },

  body: {
    fontSize: 14,
    lineHeight: 20,
    fontWeight: "400" as const,
  },

  bodyStrong: {
    fontSize: 14,
    lineHeight: 20,
    fontWeight: "600" as const,
  },

  caption: {
    fontSize: 12,
    lineHeight: 16,
    fontWeight: "400" as const,
  },

  label: {
    fontSize: 11,
    lineHeight: 14,
    fontWeight: "600" as const,
  },

  metric: {
    fontSize: 24,
    lineHeight: 30,
    fontWeight: "700" as const,
  },
} as const;
